# تصمیم‌های معماری

این سند توضیح می‌دهد چرا پروژه این‌طور ساخته شده و اگر تعداد سفارش‌ها به ۱۰۰ هزار برسد چه چیزهایی عوض می‌شود.

## چرا ساختار پروژه این‌گونه است

### جداسازی نگرانی‌ها

هر پوشه در `src/` یک کار مشخص انجام می‌دهد و فقط همان کار را:

- `types/` — قراردادهای داده مثل Order و SortOption. هیچ منطق یا JSX ندارد.
- `constants/` — پیکربندی جدول، سورت و رنگ وضعیت. هیچ state یا side effect ندارد.
- `hooks/` — state، fetch، فیلتر، سورت و صفحه‌بندی. هیچ JSX ندارد.
- `components/` — فقط رندر بر اساس props. هیچ fetch یا منطق دامنه ندارد.
- `utils/` — توابع خالص. وابستگی به React ندارد.
- `App.tsx` — فقط ترکیب. هیچ منطق ندارد.

نتیجه اینکه هر فایل را می‌توان بدون خواندن بقیه پروژه فهمید و جداگانه تست کرد.

### پیکربندی به جای کد

ORDER_COLUMNS، ORDER_SORT_OPTIONS و ORDER_DETAIL_FIELDS در `constants/orders.ts` هستند، نه داخل کامپوننت‌ها. دلیلش این است که افزودن یک ستون فقط ویرایش یک آرایه است و کامپوننت‌ها دست‌نخورده می‌مانند. تغییر رنگ یک وضعیت هم فقط ویرایش یک شیء است. تست رفتار جدول هم یعنی تست داده، نه DOM.

برای Customers یا Invoices فقط یک فایل constants جدید لازم است.

### پلی‌مورفیسم با Generics

`OrdersTable<T>` و `DetailsModal<T>` به نوع Order وابسته نیستند. هر شکلی که id داشته باشد را رندر می‌کنند. یعنی همان کامپوننت برای Customer هم بدون تغییر کار می‌کند. منطق رندر یک بار نوشته می‌شود و باگ‌ها در یک جا رفع می‌شوند.

### ترکیب به جای ارث‌بری

OrderDetailsModal از DetailsModal ارث‌بری نمی‌کند، آن را ترکیب می‌کند و از طریق renderHeader و renderFooter سرصفحه و پاصفحه اختصاصی سفارش را تزریق می‌کند. این همان الگویی است که خود مستندات React توصیه می‌کند. DetailsModal بسته به تغییر و باز به گسترش می‌ماند و از زنجیره ارث‌بری شکننده که در React ضدالگو است دوری می‌کنیم.

### state در هوک‌ها، UI در کامپوننت‌ها

فیلتر، سورت، صفحه‌بندی و media query همه در `src/hooks/` هستند. کامپوننت‌ها controlled هستند و فقط props می‌گیرند و event می‌فرستند. مزیتش این است که منطق بدون رندر تست می‌شود، همان هوک برای هر جدول دیگری کار می‌کند، و رندر فقط تابعی از props است که debug را آسان می‌کند.

### App.tsx فقط orchestrator است

تمام state و منطق در `useOrdersTable()` جمع شده. App.tsx فقط چهار کامپوننت را کنار هم می‌چیند و یک state محلی برای مودال دارد. هرچه در App.tsx کمتر باشد پروژه سالم‌تر است.

### طراحی واکنش‌گرا به عنوان یک تصمیم صریح

`useMediaQuery` دو هوک دارد: useIsMobile و useIsTablet. هیچ CSS media query پراکنده‌ای در پروژه نیست. تصمیم‌های واکنش‌گرا در JSX و در یک نقطه گرفته می‌شوند. TableToolbar روی موبایل عمودی می‌شود، OrdersTable ستون‌های کم‌اهمیت را با hideOnMobile حذف می‌کند، و DetailsModal روی موبایل fullscreen می‌شود. چون این تصمیم‌ها در JS هستند می‌توان هوک را mock کرد و رفتار را unit test کرد.

### دسترس‌پذیری به عنوان پیش‌فرض

هر دکمه آیکون‌تنها aria-label دارد. Mantine به‌صورت خودکار focus ring، keyboard navigation و احترام به prefers-reduced-motion را مدیریت می‌کند. دسترس‌پذیری یک ویژگی اضافه نیست، حق پایه کاربر است و افزودن آن بعد از نوشتن پروژه گران‌تر تمام می‌شود.

---

## اگر پروژه ۱۰۰ هزار سفارش داشته باشد چه تغییری می‌دهیم

با ۱۰۰ هزار رکورد همه فرض‌های فعلی می‌شکنند. تغییرات را به ترتیب اولویت مرور می‌کنیم.

### صفحه‌بندی سمت سرور

الان همه ۳۰ رکورد در حافظه مرورگر است و usePagination آرایه را slice می‌کند. با ۱۰۰ هزار رکورد، ارسال کل آرایه به مرورگر قابل قبول نیست. JSON خام حدود ۱۰ تا ۱۵ مگابایت می‌شود که زمان بارگذاری اولیه را به چند ثانیه می‌رساند، حافظه مرورگر را پر می‌کند و روی موبایل ممکن است کرش کند.

راه‌حل این است که usePagination را حذف کنیم یا فقط به یک state-only هوک تبدیلش کنیم، درخواست به سرور با page و limit بفرستیم، totalRecords را از response.total بگیریم نه از orders.length، و هر تغییر صفحه یک درخواست جدید باشد.

### فیلتر و سورت سمت سرور

الان useFilteredOrders کل آرایه را در حافظه فیلتر و سورت می‌کند. روی ۳۰ رکورد زیر یک میلی‌ثانیه است. با ۱۰۰ هزار رکورد، فیلتر کردن ۱۰۰ هزار شیء در هر تایپ کاربر کندی محسوس می‌سازد حتی با debounce.

فیلتر، سرچ و سورت همه باید به query params منتقل شوند. جست‌وجو روی ایندکس سرور اجرا می‌شود و سورت توسط دیتابیس با ایندکس مناسب انجام می‌شود.

### Virtual scrolling

اگر سرور مثلاً ۱۰۰ رکورد در هر صفحه برگرداند، هنوز رندر ۱۰۰ سطر tr می‌تواند روی موبایل کند باشد. راه‌حل استفاده از virtual scrolling با react-virtual یا قابلیت بومی mantine-datatable است تا فقط سطرهای داخل viewport رندر شوند.

### کش سمت کلاینت

با صفحه‌بندی سرور، هر تغییر صفحه یک درخواست HTTP است. بدون کش، کاربر با هر بازگشت به صفحه قبلی درخواست تکراری می‌فرستد و جدول در هر تغییر صفحه پرش می‌کند.

راه‌حل React Query یا SWR است با queryKey شامل page، search، status و sort برای کش هر ترکیب، staleTime برای جلوگیری از refetch غیرضروری، و keepPreviousData برای نمایش داده قبلی تا رسیدن داده جدید.

### debounce قوی‌تر و AbortController

الان debounce ۳۰۰ میلی‌ثانیه در SearchBar است. با ۱۰۰ هزار رکورد اگر کاربر سریع تایپ کند چند درخواست موازی به سرور می‌رود و پاسخ‌ها ممکن است خارج از ترتیب برسند.

راه‌حل استفاده از AbortController برای لغو درخواست‌های قدیمی و انتقال debounce به useOrdersTable است تا همه منطق درخواست در یک جا باشد.

### اعتبارسنجی طرح داده

با ۱۰۰ هزار رکورد احتمال داده ناسازگار بالا می‌رود. یک رکورد با status ناشناس نباید UI را کرش کند. راه‌حل schema با zod روی پاسخ سرور است تا رکوردهای نامعتبر لاگ و نادیده گرفته شوند.

### Loading state غنی‌تر

الان یک Loader ساده داریم. با ۱۰۰ هزار رکورد بارگذاری اولیه ممکن است دو سه ثانیه طول بکشد و Loader تنها تجربه بدی می‌سازد.

راه‌حل skeleton به جای Loader است تا کاربر ساختار جدول را ببیند، جدا کردن isLoading از isFetching برای نمایش progress bar بدون پنهان کردن داده، و retry خودکار با backoff نمایی.

### ایندکس‌گذاری و کارایی سمت سرور

این بخش تصمیم بک‌اند است اما مهم است بدانید. ایندکس روی id، customer، status و date در دیتابیس. صفحه‌بندی بر پایه cursor به جای OFFSET که روی جداول بزرگ کند می‌شود. و کش سمت سرور مثل Redis برای queryهای پرتکرار.

### کش سمت سرور و CDN

کش کردن پاسخ‌های api/orders در CDN با Cache-Control مناسب باعث می‌شود درخواست‌های تکراری هرگز به دیتابیس نرسند.

### Real-time updates

اگر داده به‌سرعت تغییر می‌کند مثل داشبورد اپراتور، می‌توان از WebSocket یا SSE برای به‌روزرسانی زنده استفاده کرد، یا polling دوره‌ای با refetchInterval در React Query.

## نکته پایانی

آن‌چه تغییر نمی‌کند OrdersTable، DetailsModal، ساختار constants و طراحی responsive است. آن‌چه تغییر می‌کند لایه داده و هوک‌های مرتبط با آن است.

این تفکیک دقیقاً هدف معماری است. جایگزین کردن منبع داده نباید به بازنویسی UI منجر شود.
